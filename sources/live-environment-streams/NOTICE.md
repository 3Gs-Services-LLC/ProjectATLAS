# NOTICE — Not cleared for redistribution

**Status as of 2026-08-24: the 5,997 records in this directory (`streams-2026-08-23.geojson`, `sources-2026-08-23.json`) are NOT cleared for public redistribution.**

`willytop8/Live-Environment-Streams` on GitHub has no `LICENSE` file (`license.spdx_id` is `null` per the GitHub API). Under GitHub's Terms of Service and default copyright law, that means the repository is all-rights-reserved by default: viewing the data is permitted, but no license to copy, redistribute, or build a derivative public dataset from it has been granted.

This data was fetched during Phase 1 source research (see `docs/research/PHASE1-SOURCE-VERIFICATION.md` §12) for evidence and technical-verification purposes, consistent with `MacEvil.md`'s research-and-verification phase. It was not fetched with an understanding that redistribution was already cleared, and it is not cleared now.

**Do not:**
- Treat any record in this directory as production-ready or `POLICY_VERIFIED` per `MacEvil.md` §9's pipeline.
- Surface this data through any public API, map, or downstream product before this notice is resolved.
- Assume the individual per-source `license_type` values inside `sources.json` (e.g. `public_domain` for VDOT) resolve this — those describe the *original operators'* licenses, not `willytop8`'s own compiled-dataset license, which is the actual gate on redistributing this specific copy.

**Resolution tracked at:** [GitHub Issue #7](https://github.com/3Gs-Services-LLC/ProjectATLAS/issues/7) — "Request license/permission: Live-Environment-Streams GitHub repo (no LICENSE file)."

This notice should be removed (and the data's status updated to reflect whatever was actually granted) only once Issue #7 is closed with a positive answer, or the data is independently re-sourced/re-licensed from original operators directly.
